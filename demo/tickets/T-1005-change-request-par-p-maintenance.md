# T-1005 — Change request: line card maintenance on Paris ethernet-1/2

From: change management

## Description

Requested window: Tuesday 02:00–04:00.

Field engineering needs to take interface `ethernet-1/2` on node
`clab-orch-demo-par-p` out of service for about two hours to replace an
optic. Before approval we need to know: which service (subscription) runs
over that port and for which customer, whether it is currently healthy and
in use, whether traffic between the affected sites has an alternate path
during the window, and which subscriptions should be flagged as under
maintenance. Nothing is to be changed now; this is an impact assessment.
