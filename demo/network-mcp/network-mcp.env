# SR Linux nodes of the containerlab topology (clab/srlinux01.clab.yaml),
# reachable by container hostname on the shared compose network.
NETWORK_NODES=clab-orch-demo-ams-pe,clab-orch-demo-par-p,clab-orch-demo-lon-pe
# containerlab's default SR Linux credentials
SRL_USERNAME=admin
SRL_PASSWORD=NokiaSrl1!
# orchestrator API, used to read the core link subscriptions (intent) for check_core_links
ORCHESTRATOR_URL=http://orchestrator:8080
