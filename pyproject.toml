[project]
name = "example-orchestrator"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "orchestrator-core==4.4.0rc3",
]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.flake8]
max-line-length = 120
